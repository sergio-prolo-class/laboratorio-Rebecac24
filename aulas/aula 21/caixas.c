#include <stdio.h>

int main()
{
    float altura;
    float largura;
    float profundidade;
    float area;
    float volume;
    printf("Digite a altura (m)");
    scanf("%f", &altura);
    printf("Digite a largura (m)");
    scanf("%f", &largura);
    printf("Digite a profundidade (m)");
    scanf("%f", &largura);
    return 0;
}
