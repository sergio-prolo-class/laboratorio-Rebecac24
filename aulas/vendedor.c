#include <stdio.h>

int main()
{
    // Variáveis
    float vendas, salario, comissao;
    int desempenho;
    salario = 20000;
    printf("Entre com o total de vendas: ");
    scanf(" %f", &vendas);
    printf("Escreva a avaliação de desempenho(1-10):");
    scanf(" %d", &desempenho);
    if (vendas >= 10000)
        ;
    {
        comissao = vendas * 0.07;
    }
}