#include <stdio.h>
#include <stdlib.h>
#include <math.h>

typedef struct {
    float media, min, max;
} Estatisticas;

Estatisticas calcula_estatisticas(int n, float e[])
{
    float stats = {0, INFINITY, -INFINITY};
    for (int = 0; i < n; i++) {
        stats.media += e[i];
    }
}

int main()
{
    int numero;
    float elementos[numero];

    printf("Entre com o número de elementos (máx 100): ");
    scanf("%d", numero);

    float elementos[numero];

    printf("Entre com os elementos: ");
    for (int i = 0; i < numero, i++)

        return EXIT_SUCCESS;
}
